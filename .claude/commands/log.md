Add this update to log.exe (facts SoT), then refresh master-resume.tex: $ARGUMENTS

See `AGENTS.md`. Prefer MCP / `logexe` writes first; TeX is layout/cache only.

Steps:
1. Write the fact into the right log.exe surface (experience / skills /
   projects / clients / certs). Ask which surface if unclear.
2. Mirror into `master-resume.tex`:
   - New achievement on an existing project/job → new or improved bullet
   - New number for an existing bullet → strengthen that bullet
   - New skill/tool → add to the right Skills category
   - Brand new project → new entry (ask for stack, dates, what it does)
3. Rewrite into a proper resume bullet: action verb + what + tool/method +
   measurable result. Keep MY facts exactly — do not inflate. If vague,
   ask one or two short questions first.
4. If replacing a weaker TeX bullet, keep the old one as a LaTeX comment (%).
5. Use precise, truthful industry keywords where they fit.
6. Compile master-resume.tex, show log.exe + TeX before/after.
7. Remind me if this is worth a LinkedIn post/headline — one sentence only
   if genuinely worth it.
